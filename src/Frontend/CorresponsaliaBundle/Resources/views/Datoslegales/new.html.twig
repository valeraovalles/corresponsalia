{% extends '::base.html.twig' %}

{% block title %}CORRESPONSALÍA - DATOS LEGALES POR CORRESPONSAL&Iacute;A{% endblock %}

{% block titulobanner %}
    DATOS LEGALES POR CORRESPONSAL&Iacute;A
{% endblock %}

{% block titulomodulo %}
    <h1>CREAR DATOS LEGALES</h1>
{% endblock %}

{% block body -%}
    {{ parent() }}
    <h3>{{ corresponsalia.nombre }} | {{ corresponsalia.pais }}</h3>
     <form novalidate method="post" action="{{ path('datoslegales_create',{'corresponsalia':corresponsalia.id}) }}">
        {{ form_widget(form._token) }}{{ form_widget(form1._token) }}
        <div class="formShow" style="width:70%;">
            <div class="contenedorform">
                <div class="labelform">Representante Legal:</div>
                <div class="widgetform">{{ form_widget(form1.representante) }}</div>
            </div>
            <div class="contenedorform">
                <div class="labelform">Direcci&oacute;n:</div>
                <div class="widgetform">{{ form_widget(form.direccion, { 'attr': {'style': 'width:100%;height:300px;'} }) }}</div>
            </div>
            <div class="contenedorform">
                <div class="labelform">Datos del Registro:</div>
                <div class="widgetform">{{ form_widget(form.registro, { 'attr': {'style': 'width:100%;height:300px;'} }) }}</div>
            </div>

        </div>
        <input type="submit" value="CREAR" class="btn btn-primary"> | 
        <a class="btn btn-default" href="{{ path('datoslegales') }}">IR AL LISTADO</a>
    </form>
{% endblock %}
