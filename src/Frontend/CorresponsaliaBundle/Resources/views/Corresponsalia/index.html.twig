{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Corresponsalia list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Montocambiodolar</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('corresponsalia_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.montocambiodolar }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('corresponsalia_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('corresponsalia_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('corresponsalia_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
