{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pais list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Pais</th>
                <th>Referencia</th>
                <th>Latitud</th>
                <th>Longitud</th>
                <th>Codigo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('xx_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.pais }}</td>
                <td>{{ entity.referencia }}</td>
                <td>{{ entity.latitud }}</td>
                <td>{{ entity.longitud }}</td>
                <td>{{ entity.codigo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('xx_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('xx_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('xx_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
