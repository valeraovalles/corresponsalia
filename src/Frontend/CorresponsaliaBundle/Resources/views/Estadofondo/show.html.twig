{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Estadofondo</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Saldoinicial</th>
                <td>{{ entity.saldoinicial }}</td>
            </tr>
            <tr>
                <th>Recursorecibido</th>
                <td>{{ entity.recursorecibido }}</td>
            </tr>
            <tr>
                <th>Anio</th>
                <td>{{ entity.anio }}</td>
            </tr>
            <tr>
                <th>Mes</th>
                <td>{{ entity.mes }}</td>
            </tr>
            <tr>
                <th>Saldofinal</th>
                <td>{{ entity.saldofinal }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('estadofondo') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('estadofondo_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
